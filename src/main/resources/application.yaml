spring:
  application:
    name: demo


  datasource:
    url: jdbc:postgresql://localhost:5432/synccast
    username: postgres
    password: Dp810@310
    driver-class-name: org.postgresql.Driver


  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
